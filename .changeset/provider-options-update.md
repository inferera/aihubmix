---
"@aihubmix/ai-sdk-provider": patch
---

- 添加 `aihubmixChatProviderOptionsSchema` 和 `aihubmixResponsesProviderOptionsSchema` 以支持所有 Vercel AI SDK OpenAI 提供商选项。
- 从主入口点导出选项类型和模式。
- 改进了 `gpt-5-pro` 和 `gpt-5-codex` 的模型配置。
- 通过更好的文件命名逻辑增强了 `AihubmixTranscriptionModel`。
